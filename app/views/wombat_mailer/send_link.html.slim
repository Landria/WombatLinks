doctype html
html
  head
    meta content="text/html; charset=UTF-8" http-equiv="Content-Type"
  body
    = render :partial => 'wombat_mailer/header'
    p
      strong= (t :email_new_link_text)
    p
      strong
        ' URL:
        = link_to(@link.link, @link.link)
      br
      - if(!@link.title.empty?)
        strong= t :title
        ' :
        = raw @link.title
        br
      - if(!@link.description.empty?)
      strong= t :description
      ' :
      =h @link.description
    br
    p= link_to (t :spam_complain_message), spam_complain_path(:hash => @link.hash, :only_path => false)
    = render :partial =>'wombat_mailer/footer'

