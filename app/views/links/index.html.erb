<div class="row twelve columns">
<div id="title_block">
  <% if signed_in? %>
    <% if(!@all)%>
      <h1><%= t :my_links %></h1> 
      <%= link_to (t :all_links), all_links_path, :method => :post %> 
    <% else %>
      <h1><%= t :all_links %></h1>
      <%= link_to (t :my_links), links_path %>
    <%end%>
  <% else %>
  <h1><%= t :all_links %></h1>
  <h5>Вы просматриваете анонимные ссылки. Для просмотра всех ссылок авторизуйтесь.</h5>
  <% end %>
</div>
  <% @links.each do |link| %>
  <p>
    <b><%= (t :link)+' #'+ link.id.to_s%>:</b> <%= link_to(link.link, link.link) %>
  </p>
  <p>
    <b><%= (t :title)%>:</b> <%= link.title %>
  </p>
  <p>
    <b><%= (t :description)%>:</b> <%= link.description %>
  </p>
  <p>
    <b><%= (t :email)%>:</b> <%= link.email %>
  </p>
  <% if link.is_private%>
  <p>
    <b><%= (t :is_private_show)%>:</b>
  </p>
  <% end %>
  <div class="link_controls">
  <%= link_to 'Show', link %>
  <% if can? :manage, Link %>
    | <%= link_to 'Edit', edit_link_path(link) %>
    | <%= link_to 'Destroy', link, :confirm => 'Are you sure?', :method => :delete %>
  <% end %>
  </div>
  <hr>
  <% end %>
  
  <% if(@links.count == 0)%>
    <p><%= t :no_links%></p>
  <% end %>

  <% if can? :create, Link %>
  <p><%= link_to (t :new_link), new_link_path, :class => "nice radius white button" %></p>
  <% end %>
</div>
